"""Data ingestion and preprocessing utilities for PaySim.

This package is used both by Airflow DAGs (imported as `data.*` when `/opt/airflow/src`
 is on `sys.path`) and by unit tests (imported as `src.data.*`).
"""
